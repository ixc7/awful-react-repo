{
  "name": "my-react-app",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "5.0.0",
    "sass": "^1.49.7"
  },
  "scripts": {
    "stop": "killall node",
    "start": "cleanup () { printf \"\\x1b[?25h\" ; } && trap cleanup EXIT && printf \"\\x1Bc\\x1b[3J\\x1b[?25l\" && react-scripts start > /dev/null 2>&1 & pnpm run dev",
    "dev": "${EDITOR} src/components/* src/scss/*",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "lint": "standard --fix"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "standard": "^16.0.4"
  }
}
